<Page x:Class="MasterP.Page3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MasterP"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Просмотр истории реализации"
      FontFamily="Segoe UI">

    <Grid>
        <Rectangle Fill="#F4E8D3" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

        <Image Source="Resource/Мастер пол.png" Width="100" Height="100" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Stretch="UniformToFill"/>

        <TextBlock Text="История реализации" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,0"/>

        <ComboBox x:Name="cmbPartners" Width="250" Height="30" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0"/>

        <TextBlock x:Name="txtDateTime" FontSize="14" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,50,10,0"/>

        <Button Content="Назад" Width="80" Height="30" Background="White" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0" Click="BackButton_Click"/>

        <DataGrid x:Name="dataGridHistory" AutoGenerateColumns="True" Margin="115,102,132,41" IsReadOnly="True"/>
    </Grid>
</Page>